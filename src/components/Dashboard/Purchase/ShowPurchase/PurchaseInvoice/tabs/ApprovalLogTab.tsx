"use client";

import ApprovalTimeline from "../../sections/ApprovalTimeline";

export default function ApprovalLogTab({ events }: { events: any[] }) {
  // return <ApprovalTimeline events={events} title="Approval Log" />;
  return null;
}
